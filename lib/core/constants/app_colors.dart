import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF1100FF);
  static const Color primaryLight = Color(0xFF4436FF);
  static const Color primaryDark = Color(0xFF0A00CC);
  static const Color accentPink = Color(0xFFF43F5E);
  static const Color background = Colors.white;
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textLight = Color(0xFF9CA3AF);
  static const Color border = Color(0xFFE5E7EB);
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  // // Main brand colors mapped to the new navy/blue palette
  // static const Color primary = Color(0xFF052659);       // Deep Blue (#052659)
  // static const Color primaryLight = Color(0xFF5483B3);  // Muted Slate Blue (#5483B3)
  // static const Color primaryDark = Color(0xFF021024);   // Deep Navy/Almost Black (#021024)
  // static const Color accentPink = Color(0xFFC1E8FF);    // Replaced pink with Ice Blue (#C1E8FF)

  // // Layout & UI surfaces
  // static const Color background = Color(0xFFF4F9FD);    // Soft blue-tinted white for harmony
  // static const Color surface = Colors.white;            // Pure white for cards/inputs

  // // Typography mapped to the new palette tones
  // static const Color textPrimary = Color(0xFF021024);   // Deep Navy instead of dark gray
  // static const Color textSecondary = Color(0xFF5483B3); // Slate Blue instead of mid-gray
  // static const Color textLight = Color(0xFF7DA0CA);     // Light Steel Blue instead of light gray

  // // Component elements & feedback
  // static const Color border = Color(0xFFE1EDF7);        // Cool-tinted border
  // static const Color success = Color(0xFF10B981);       // Kept standard functional green
  // static const Color error = Color(0xFFEF4444);         // Kept standard functional red
}
